VOICE INTERACTION
- When the gemini-2.5-flash model (for reasoning) daily calls are exhausted, switch automatically to gemini-2.0-flash.
    - If it can't be done, or when the gemini-2.0-flash model exhausted the daily calls, stop the voice interaction
    service.
- When the gemini-2.5-flash-preview-tts model (for Text-To-Speech) daily calls are exhausted execute all the points
below. All of them are already implemented, but have never been tested. In particular, it is not clear if the code for
detecting daily-calls limit works or not.
    - Print responses in the console, in case the user connects remotely to the robot or a screen is connected to the
     robot.
    - Specifically print to console the last sentence (the one that gave the error/signal of finished usage) because it
     would otherwise not be played nor written.
    - Play a pre-recorded audio that notifies the user of the situation.

HARDWARE
- Add hardware for measuring the charge of the battery connected to the Jetson NANO (through the expansion board),
since it has no ability to do so.
    - Then add code to monitor and handle the battery level.
        - Signal user with sound when battery charge is too low.
        - Power off the Jetson NANO when battery charge is too low.
- Buy an usb-modem (to pair with the internet sim) so that the robot will have automatic internet access even outside
a wi-fi network.
    - Configure the two computers so that both will automatically connect to this network if the default wi-fi is not
     reachable.
    - Save the new image of both computers.
- Add a small touch screen to the robot. It helps find bugs when the robot is not connected to pc.
    - Make an app for the screen, so the main functions of the robot can be called from the small touch screen even
    in the absence of a controller.
- Add a motor to control the vertical orientation of the frontal camera
    - Add code to control the motor with the controller.
    - Add code to control the motor in autonomous mode (vision): if the target is found, try to center it. Also try to
    account for the possible forward movement.

AUTONOMOUS MODE (VISION)
- When in autonomous mode (vision), when the lidar is connected, when the target is found, when it is close enough
(basically when the current robot emits a beep):
    - If the speakers are connected, play a short victory tune.
    - Exit autonomous mode and return to user controlled mode (wheels).

AUTONOMOUS MODE (SOUND)
- If autonomous mode (sound) only reacts to voices, it should fix the problem of hearing and going towards its own
motors.
    - Is it worse for the user if it only reacts to voices?
- If the lidar is connected, automatically use it for obstacle avoidance in autonomous mode (sound).
- Create a second autonomous mode (sound) in which the robot runs away from sounds. Better if it is only voices, because
at the moment it will just run away from its rear motors, meaning it will just run straight into some obstacle.

VR APP
- Make streaming parameters modifiable.
- The app has no icon in the VR bottom bar.
- Add "attached mode" to the menù window, so that it can fix itself to the camera window and become a single window.
- button "activate/deactivate": display it in gray and not clickable if no messages are being received on the
currently selected topic
    - No messages, or the last received message is older than n seconds.

GENERIC
- Finish the website.
- Controller interface should be more generic, like only providing functions. It should be ps2_controller and
other equivalent classes that associate functions with specific buttons.
- Add the option to save log to file, to help debugging.
